# Container site (host). For Policy Details and Pay Premium sites, configure in Netlify dashboard.
# See Deployment.md for three-site setup and env vars.

[build]
  command = "npm run build -w container"
  publish = "container/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
